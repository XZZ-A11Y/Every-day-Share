name: 自动回复
on:
  issues:
    types: [opened]
jobs:
  reply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: "👋 已收到投稿，关闭即自动发布（会注明贡献者与收费信息）。"
            });
